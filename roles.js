module.exports = {
    builder: require('role.builder'),
    harvester: require('role.harvester'),
    upgrader: require('role.upgrader'),
    scavenger: null //TODO: create scavenger role to pick up stuff?
}
